# Configurações do Sistema de Gerenciamento de Pedidos (SGP)
# Copie este arquivo para .env e configure com suas credenciais

# ===========================================
# CONFIGURAÇÕES DA API
# ===========================================
# URL base da API Python FastAPI (usado na tela de configuração)
VITE_API_URL=http://192.168.0.10:8000
# URL default para versão web (ex: https://api.seudominio.com)
VITE_API_BASE_URL=

# Versão do app (para exibição na web; desktop usa versão do Tauri)
# VITE_APP_VERSION=1.2.6

# Para deploy em subpath (ex: GitHub Pages em /sgp/), defina:
# VITE_BASE_PATH=/sgp/

# ===========================================
# CONFIGURAÇÕES DO BANCO DE DADOS (Rust Backend - Descontinuado)
# ===========================================

# Opção 1: URL completa de conexão (RECOMENDADO)
# Formato: postgresql://usuario:senha@host:porta/nome_do_banco
DATABASE_URL=postgresql://usuario:senha@localhost:5432/sgp_v4

# Opção 2: Configurações individuais (caso prefira usar variáveis separadas)
# Descomente as linhas abaixo e comente a DATABASE_URL acima
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=sgp_v4
# DB_USER=usuario
# DB_PASSWORD=senha

# Configurações avançadas do banco (opcional)
# DB_MAX_CONNECTIONS=5

# ===========================================
# CONFIGURAÇÕES DE MIGRAÇÕES
# ===========================================

# Controla se as migrações devem ser executadas automaticamente
# true = executa migrações na inicialização (desenvolvimento)
# false = apenas conecta ao banco (produção)
RUN_MIGRATIONS=true

# Ambiente da aplicação
# development = modo desenvolvimento (executa migrações por padrão)
# production = modo produção (não executa migrações por padrão)
APP_ENV=development

# ===========================================
# CONFIGURAÇÕES DA APLICAÇÃO
# ===========================================

# Configurações de Log (opcional)
# RUST_LOG=info

# Configurações de Sessão (opcional)
# SESSION_TIMEOUT_HOURS=12

# Configurações de Cache (opcional)
# CACHE_TTL_SECONDS=300

# ===========================================
# INSTRUÇÕES DE USO
# ===========================================
# 1. Copie este arquivo para .env
# 2. Configure pelo menos uma das opções de banco de dados:
#    - DATABASE_URL (recomendado) OU
#    - DB_USER e DB_PASSWORD (mínimo necessário)
# 3. Configure RUN_MIGRATIONS:
#    - true para desenvolvimento (executa migrações)
#    - false para produção (apenas conecta)
# 4. Execute o aplicativo
#
# ===========================================
# EXEMPLOS DE CONFIGURAÇÃO
# ===========================================
#
# 🖥️ DESENVOLVIMENTO:
# DATABASE_URL=postgresql://finderbit:senha123@localhost:5432/sgp
# RUN_MIGRATIONS=true
# APP_ENV=development
#
# 🏭 PRODUÇÃO:
# DATABASE_URL=postgresql://usuario:senha@192.168.15.9:5432/sgp
# RUN_MIGRATIONS=false
# APP_ENV=production
